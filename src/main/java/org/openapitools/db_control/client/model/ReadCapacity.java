/*
 * Pinecone Control Plane API
 * Pinecone is a vector database that makes it easy to search and retrieve billions of high-dimensional vectors.
 *
 * The version of the OpenAPI document: 2025-10
 * Contact: support@pinecone.io
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.db_control.client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import org.openapitools.db_control.client.model.ReadCapacityDedicatedConfig;
import org.openapitools.db_control.client.model.ReadCapacityDedicatedSpec;
import org.openapitools.db_control.client.model.ReadCapacityOnDemandSpec;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import org.openapitools.db_control.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-11-12T17:52:29.716713Z[Etc/UTC]")
public class ReadCapacity extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(ReadCapacity.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!ReadCapacity.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'ReadCapacity' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<ReadCapacityOnDemandSpec> adapterReadCapacityOnDemandSpec = gson.getDelegateAdapter(this, TypeToken.get(ReadCapacityOnDemandSpec.class));
            final TypeAdapter<ReadCapacityDedicatedSpec> adapterReadCapacityDedicatedSpec = gson.getDelegateAdapter(this, TypeToken.get(ReadCapacityDedicatedSpec.class));

            return (TypeAdapter<T>) new TypeAdapter<ReadCapacity>() {
                @Override
                public void write(JsonWriter out, ReadCapacity value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `ReadCapacityOnDemandSpec`
                    if (value.getActualInstance() instanceof ReadCapacityOnDemandSpec) {
                      JsonElement element = adapterReadCapacityOnDemandSpec.toJsonTree((ReadCapacityOnDemandSpec)value.getActualInstance());
                      elementAdapter.write(out, element);
                      return;
                    }
                    // check if the actual instance is of the type `ReadCapacityDedicatedSpec`
                    if (value.getActualInstance() instanceof ReadCapacityDedicatedSpec) {
                      JsonElement element = adapterReadCapacityDedicatedSpec.toJsonTree((ReadCapacityDedicatedSpec)value.getActualInstance());
                      elementAdapter.write(out, element);
                      return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: ReadCapacityDedicatedSpec, ReadCapacityOnDemandSpec");
                }

                @Override
                public ReadCapacity read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize ReadCapacityOnDemandSpec
                    try {
                      // validate the JSON object to see if any exception is thrown
                      ReadCapacityOnDemandSpec.validateJsonElement(jsonElement);
                      actualAdapter = adapterReadCapacityOnDemandSpec;
                      match++;
                      log.log(Level.FINER, "Input data matches schema 'ReadCapacityOnDemandSpec'");
                    } catch (Exception e) {
                      // deserialization failed, continue
                      errorMessages.add(String.format("Deserialization for ReadCapacityOnDemandSpec failed with `%s`.", e.getMessage()));
                      log.log(Level.FINER, "Input data does not match schema 'ReadCapacityOnDemandSpec'", e);
                    }
                    // deserialize ReadCapacityDedicatedSpec
                    try {
                      // validate the JSON object to see if any exception is thrown
                      ReadCapacityDedicatedSpec.validateJsonElement(jsonElement);
                      actualAdapter = adapterReadCapacityDedicatedSpec;
                      match++;
                      log.log(Level.FINER, "Input data matches schema 'ReadCapacityDedicatedSpec'");
                    } catch (Exception e) {
                      // deserialization failed, continue
                      errorMessages.add(String.format("Deserialization for ReadCapacityDedicatedSpec failed with `%s`.", e.getMessage()));
                      log.log(Level.FINER, "Input data does not match schema 'ReadCapacityDedicatedSpec'", e);
                    }

                    if (match == 1) {
                        ReadCapacity ret = new ReadCapacity();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for ReadCapacity: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public ReadCapacity() {
        super("oneOf", Boolean.FALSE);
    }

    public ReadCapacity(ReadCapacityDedicatedSpec o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public ReadCapacity(ReadCapacityOnDemandSpec o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("ReadCapacityOnDemandSpec", ReadCapacityOnDemandSpec.class);
        schemas.put("ReadCapacityDedicatedSpec", ReadCapacityDedicatedSpec.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return ReadCapacity.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * ReadCapacityDedicatedSpec, ReadCapacityOnDemandSpec
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof ReadCapacityOnDemandSpec) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof ReadCapacityDedicatedSpec) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be ReadCapacityDedicatedSpec, ReadCapacityOnDemandSpec");
    }

    /**
     * Get the actual instance, which can be the following:
     * ReadCapacityDedicatedSpec, ReadCapacityOnDemandSpec
     *
     * @return The actual instance (ReadCapacityDedicatedSpec, ReadCapacityOnDemandSpec)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `ReadCapacityOnDemandSpec`. If the actual instance is not `ReadCapacityOnDemandSpec`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ReadCapacityOnDemandSpec`
     * @throws ClassCastException if the instance is not `ReadCapacityOnDemandSpec`
     */
    public ReadCapacityOnDemandSpec getReadCapacityOnDemandSpec() throws ClassCastException {
        return (ReadCapacityOnDemandSpec)super.getActualInstance();
    }
    /**
     * Get the actual instance of `ReadCapacityDedicatedSpec`. If the actual instance is not `ReadCapacityDedicatedSpec`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `ReadCapacityDedicatedSpec`
     * @throws ClassCastException if the instance is not `ReadCapacityDedicatedSpec`
     */
    public ReadCapacityDedicatedSpec getReadCapacityDedicatedSpec() throws ClassCastException {
        return (ReadCapacityDedicatedSpec)super.getActualInstance();
    }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to ReadCapacity
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
    // validate oneOf schemas one by one
    int validCount = 0;
    ArrayList<String> errorMessages = new ArrayList<>();
    // validate the json string with ReadCapacityOnDemandSpec
    try {
      ReadCapacityOnDemandSpec.validateJsonElement(jsonElement);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for ReadCapacityOnDemandSpec failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    // validate the json string with ReadCapacityDedicatedSpec
    try {
      ReadCapacityDedicatedSpec.validateJsonElement(jsonElement);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for ReadCapacityDedicatedSpec failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    if (validCount != 1) {
      throw new IOException(String.format("The JSON string is invalid for ReadCapacity with oneOf schemas: ReadCapacityDedicatedSpec, ReadCapacityOnDemandSpec. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
    }
  }

 /**
  * Create an instance of ReadCapacity given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of ReadCapacity
  * @throws IOException if the JSON string is invalid with respect to ReadCapacity
  */
  public static ReadCapacity fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, ReadCapacity.class);
  }

 /**
  * Convert an instance of ReadCapacity to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

